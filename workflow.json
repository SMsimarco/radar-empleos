{
  "name": "Radar de empleos",
  "nodes": [
    {
      "typeVersion": 1.2,
      "position": [
        -200,
        200
      ],
      "id": "cron-diario",
      "name": "Cron diario",
      "type": "n8n-nodes-base.scheduleTrigger",
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "cronExpression",
              "expression": "0 8 * * *"
            }
          ]
        }
      }
    },
    {
      "typeVersion": 2.1,
      "position": [
        80,
        100
      ],
      "id": "leer-alertas-gmail",
      "name": "Leer alertas Gmail",
      "type": "n8n-nodes-base.gmail",
      "parameters": {
        "resource": "message",
        "operation": "getAll",
        "returnAll": true,
        "filters": {
          "q": "(from:linkedin.com OR from:indeed.com OR from:computrabajo.com OR from:bumeran.com) newer_than:1d"
        },
        "simple": false
      },
      "credentials": {
        "gmailOAuth2": {
          "id": "REEMPLAZAR",
          "name": "Gmail account"
        }
      }
    },
    {
      "typeVersion": 4.5,
      "position": [
        80,
        340
      ],
      "id": "leer-ofertas-vistas",
      "name": "Leer ofertas ya vistas",
      "type": "n8n-nodes-base.googleSheets",
      "parameters": {
        "operation": "read",
        "documentId": {
          "mode": "id",
          "value": "TU_SHEET_ID_AQUI"
        },
        "sheetName": {
          "mode": "name",
          "value": "Ofertas"
        },
        "options": {}
      },
      "credentials": {
        "googleSheetsOAuth2Api": {
          "id": "REEMPLAZAR",
          "name": "Google Sheets Account"
        }
      }
    },
    {
      "typeVersion": 2,
      "position": [
        320,
        100
      ],
      "id": "parsear-ofertas",
      "name": "Parsear ofertas",
      "type": "n8n-nodes-base.code",
      "onError": "continueRegularOutput",
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// Separa cada mail de alerta en ofertas individuales — dispatcher por\n// remitente, porque cada sitio arma el mail totalmente distinto (verificado\n// contra mails reales del 2026-08-28: Indeed = 1 oferta/mail con\n// empresa+puesto en el subject; Computrabajo = batch de filas markdown\n// [| || || puesto || empresa || ubicación || fecha |](url); Bumeran = batch\n// sin link en texto plano, requiere el html del mail).\n//\n// ASUNCIÓN SIN VALIDAR (igual que el split de LinkedIn de la v1): los\n// nombres de campo item.json.from / item.json.subject / item.json.textHtml\n// son el formato esperado del nodo Gmail en esta instancia de n8n — no se\n// pudo confirmar contra una ejecución real todavía. Si \"Aplicar tope de\n// seguridad\" reporta 0 ofertas de una fuente que sabés que llegó, revisar\n// acá primero.\n//\n// El regex de Computrabajo se armó leyendo el texto plano real de un mail\n// (vía Gmail API, no vía este nodo n8n) — la conversión HTML→texto plano\n// de n8n puede diferir de la de Gmail API en espacios/formato exacto.\n// Correr una vez, mirar el output de \"Aplicar tope de seguridad\" y ajustar\n// el regex si no matchea.\n\nfunction textoDe(item) {\n  return item.json.textPlain || item.json.text || item.json.snippet || '';\n}\nfunction htmlDe(item) {\n  return item.json.textHtml || item.json.html || '';\n}\nfunction remitenteDe(item) {\n  return String(item.json.from || item.json.sender || '').toLowerCase();\n}\n\nfunction parsearLinkedIn(item) {\n  const cuerpo = textoDe(item);\n  const regexLink = /https:\\/\\/www\\.linkedin\\.com\\/comm\\/jobs\\/view\\/\\d+[^\\s)>\\]]*/g;\n  const matches = [...cuerpo.matchAll(regexLink)];\n  if (matches.length === 0) return [{ link: null, texto_oferta: cuerpo.trim(), fuente: 'linkedin' }];\n  const out = [];\n  for (let i = 0; i < matches.length; i++) {\n    const inicio = i === 0 ? 0 : matches[i - 1].index + matches[i - 1][0].length;\n    const fin = matches[i].index + matches[i][0].length;\n    out.push({ link: matches[i][0], texto_oferta: cuerpo.slice(inicio, fin).trim(), fuente: 'linkedin' });\n  }\n  return out;\n}\n\nfunction parsearIndeed(item) {\n  // 1 oferta por mail — empresa/puesto vienen en el subject, no en el cuerpo.\n  // link queda null a propósito: el link de Indeed es un redirect\n  // cts.indeed.com de un solo uso, no sirve como referencia estable. El\n  // dedup usa empresa+puesto (normalizarEmpresaPuesto), no el link.\n  const cuerpo = textoDe(item);\n  const asunto = item.json.subject || '';\n  return [{ link: null, texto_oferta: (asunto + '\\n\\n' + cuerpo).trim(), fuente: 'indeed' }];\n}\n\nfunction parsearComputrabajo(item) {\n  // Cada oferta del batch es una fila markdown:\n  // [| || || <puesto> || <empresa> || <ubicación> || <fecha> |](url)\n  const cuerpo = textoDe(item);\n  const regexFila = /\\[\\|\\s*\\|\\|\\s*\\|\\|\\s*([^|]+?)\\s*\\|\\|\\s*([^|]+?)\\s*\\|\\|\\s*([^|]+?)\\s*\\|\\|\\s*([^|]+?)\\s*\\|\\]\\(([^)]+)\\)/g;\n  const out = [];\n  let m;\n  while ((m = regexFila.exec(cuerpo)) !== null) {\n    const puesto = m[1], empresa = m[2], ubicacion = m[3], fecha = m[4], url = m[5];\n    out.push({ link: url, texto_oferta: puesto + ' — ' + empresa + ' — ' + ubicacion + ' — ' + fecha, fuente: 'computrabajo' });\n  }\n  if (out.length === 0) return [{ link: null, texto_oferta: cuerpo.trim(), fuente: 'computrabajo' }];\n  return out;\n}\n\nfunction parsearBumeran(item) {\n  // El texto plano NO trae links (se pierden al convertir el mail a texto\n  // plano) — hace falta el html. Sin validar todavía contra el HTML real:\n  // en vez de inventar una estructura, esto cae a NO_PARSEABLE (revisar a\n  // mano) hasta confirmar el HTML real y escribir el split de tarjetas.\n  const cuerpoHtml = htmlDe(item);\n  if (!cuerpoHtml) {\n    return [{ link: null, texto_oferta: '', fuente: 'bumeran', error_parseo_mail: true, detalle_error: 'Sin campo html en el item de Gmail — no se puede sacar el link de Bumeran del texto plano.' }];\n  }\n  return [{ link: null, texto_oferta: textoDe(item).trim(), fuente: 'bumeran', error_parseo_mail: true, detalle_error: 'Parser de Bumeran pendiente de validar contra HTML real — cae a revisión manual.' }];\n}\n\nconst items = [];\nfor (const item of $input.all()) {\n  try {\n    const remitente = remitenteDe(item);\n    let ofertas;\n    if (remitente.includes('linkedin.com')) ofertas = parsearLinkedIn(item);\n    else if (remitente.includes('indeed.com')) ofertas = parsearIndeed(item);\n    else if (remitente.includes('computrabajo.com')) ofertas = parsearComputrabajo(item);\n    else if (remitente.includes('bumeran.com')) ofertas = parsearBumeran(item);\n    else ofertas = [{ link: null, texto_oferta: textoDe(item).trim(), fuente: 'desconocida' }];\n\n    for (const oferta of ofertas) items.push({ json: oferta });\n  } catch (e) {\n    items.push({ json: { link: null, texto_oferta: '', fuente: 'error', error_parseo_mail: true, detalle_error: String((e && e.message) || e) } });\n  }\n}\n\nreturn items;"
      }
    },
    {
      "typeVersion": 2,
      "position": [
        540,
        100
      ],
      "id": "tope-seguridad",
      "name": "Aplicar tope de seguridad",
      "type": "n8n-nodes-base.code",
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// Tope duro de ofertas procesadas por corrida, para que un día raro (mail\n// mal filtrado, alerta que trae 200 avisos) no genere una factura\n// inesperada de la API de Claude. Cambiá este número si el volumen normal\n// de alertas crece — es la única variable que hay que tocar.\nconst MAX_OFERTAS_POR_CORRIDA = 30;\n\nconst items = $input.all();\nconst totalDetectado = items.length;\nconst excedeTope = totalDetectado > MAX_OFERTAS_POR_CORRIDA;\nconst procesar = excedeTope ? items.slice(0, MAX_OFERTAS_POR_CORRIDA) : items;\n\n// El flag va en todos los items que siguen para que \"Armar mail resumen\"\n// (que corre una sola vez al final) pueda mostrar el aviso sin nodos extra.\nreturn procesar.map((item) => ({\n  json: {\n    ...item.json,\n    _tope_alcanzado: excedeTope,\n    _total_detectado: totalDetectado,\n    _procesadas: procesar.length,\n  },\n}));"
      }
    },
    {
      "typeVersion": 2,
      "position": [
        780,
        100
      ],
      "id": "preparar-prompt-ia",
      "name": "Preparar prompt IA",
      "type": "n8n-nodes-base.code",
      "onError": "continueRegularOutput",
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "// Mirror de prompts/extraccion_oferta.md v1.0 — si cambiás el prompt,\n// actualizalo primero ahí (con los 2 ejemplos resueltos a mano) y después\n// pegalo acá.\nconst SYSTEM_PROMPT = \"Sos un extractor de datos, no un evaluador. Tu única tarea es leer el texto\\nde una oferta de trabajo y devolver JSON con los hechos que están\\nLITERALMENTE en el texto. No opines si la oferta es buena o mala. No la\\ncompares con ningún perfil. No hay perfil de candidato en este prompt a\\npropósito: si lo hubiera, ibas a sesgar la extracción.\\n\\nREGLA DURA: si un dato no aparece explícito en el texto, el campo va `null`.\\nNunca inferís, nunca estimás, nunca completás con lo que \\\"es común en este\\ntipo de aviso\\\". Un salario no mencionado es `null`, no \\\"a convenir\\\". Una\\nmodalidad no mencionada es `null`, no \\\"probablemente híbrido\\\".\\n\\nREGLA DURA sobre frases_exclusion: cada elemento del array tiene que ser una\\ncita TEXTUAL, copiada carácter por carácter del aviso original (podés\\nrecortar la oración, pero no parafrasear ni resumir). Si no hay ninguna\\nfrase que excluya candidatos por experiencia, título, o similar, el array\\nva vacío []. Ejemplos de qué SÍ calza en frases_exclusion: \\\"Please only\\napply if you have 5+ years of experience\\\", \\\"Not looking for junior\\nprofiles\\\", \\\"Master's degree required\\\". Ejemplos de qué NO va: cualquier\\nfrase que vos reformules aunque el significado sea el mismo.\\n\\nDevolvé ÚNICAMENTE el JSON, sin texto antes ni después, sin markdown, sin\\n```json. Si algo del formato de abajo no se cumple, tu output es inválido.\\n\\nEsquema exacto:\\n\\n{\\n  \\\"empresa\\\": string | null,\\n  \\\"puesto\\\": string | null,\\n  \\\"modalidad\\\": \\\"remoto\\\" | \\\"híbrido\\\" | \\\"presencial\\\" | null,\\n  \\\"ubicacion_requerida\\\": string | null,\\n  \\\"tipo_contrato\\\": \\\"full time\\\" | \\\"part time\\\" | \\\"freelance\\\" | \\\"pasantía\\\" | null,\\n  \\\"horas_semanales\\\": number | null,\\n  \\\"salario_min\\\": number | null,\\n  \\\"salario_max\\\": number | null,\\n  \\\"salario_moneda\\\": string | null,\\n  \\\"anios_experiencia_min\\\": number | null,\\n  \\\"anios_excluyente\\\": boolean | null,\\n  \\\"titulo_requerido\\\": \\\"grado\\\" | \\\"maestría\\\" | string | null,\\n  \\\"titulo_excluyente\\\": boolean | null,\\n  \\\"frases_exclusion\\\": string[],\\n  \\\"stack\\\": string[],\\n  \\\"postulantes\\\": number | null,\\n  \\\"antiguedad_horas\\\": number | null,\\n  \\\"postulacion_rapida\\\": boolean | null,\\n  \\\"link\\\": string | null,\\n  \\\"confianza_extraccion\\\": number\\n}\\n\\nanios_excluyente y titulo_excluyente van true solo si el texto dice\\nexplícitamente que ese requisito es obligatorio/excluyente (ej: \\\"required\\\",\\n\\\"mandatory\\\", \\\"must have\\\", \\\"excluyente\\\"). Si el texto lo menciona como\\ndeseable o no aclara, van false. Si el dato de experiencia/título ni\\nsiquiera aparece, van null (no false).\\n\\nconfianza_extraccion es tu propia estimación de 0 a 1 de qué tan completo y\\nsin ambigüedad estaba el texto fuente para esta extracción puntual. No mide\\nsi la oferta es buena, mide si vos pudiste leerla bien.\";\n\nconst userPrompt = `Extraé los datos de esta oferta según las reglas del system prompt.\n\nLINK: ${$json.link}\n\nTEXTO DE LA OFERTA:\n${$json.texto_oferta}`;\n\nreturn {\n  json: {\n    ...$json,\n    systemPrompt: SYSTEM_PROMPT,\n    userPrompt,\n  },\n};"
      }
    },
    {
      "typeVersion": 4.2,
      "position": [
        1020,
        100
      ],
      "id": "extraer-datos-ia",
      "name": "Extraer datos con IA",
      "type": "n8n-nodes-base.httpRequest",
      "onError": "continueErrorOutput",
      "retryOnFail": true,
      "maxTries": 4,
      "waitBetweenTries": 3000,
      "parameters": {
        "method": "POST",
        "url": "https://api.anthropic.com/v1/messages",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "anthropic-version",
              "value": "2023-06-01"
            },
            {
              "name": "content-type",
              "value": "application/json"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ model: \"claude-haiku-4-5\", max_tokens: 1024, temperature: 0, system: $json.systemPrompt, messages: [{ role: \"user\", content: $json.userPrompt }] }) }}",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "options": {
          "timeout": 30000
        }
      },
      "credentials": {
        "httpHeaderAuth": {
          "id": "REEMPLAZAR",
          "name": "Anthropic API Key (x-api-key)"
        }
      }
    },
    {
      "typeVersion": 2,
      "position": [
        1260,
        260
      ],
      "id": "marcar-fallo-ia",
      "name": "Marcar fallo llamada IA",
      "type": "n8n-nodes-base.code",
      "onError": "continueRegularOutput",
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "// Se llega acá cuando \"Extraer datos con IA\" agotó los reintentos (429,\n// 529, timeout, error de red). No hay respuesta del modelo para parsear —\n// se marca directo como fallo de extracción, mismo tratamiento que un JSON\n// inválido en \"Parsear respuesta IA\", para no perder la oferta.\nconst linkOriginal = $('Parsear ofertas').item.json.link;\nreturn {\n  json: {\n    error_extraccion: true,\n    link: linkOriginal,\n    respuesta_cruda: { error: ($json && $json.error) || $json },\n  },\n};"
      }
    },
    {
      "typeVersion": 2,
      "position": [
        1260,
        100
      ],
      "id": "parsear-respuesta-ia",
      "name": "Parsear respuesta IA",
      "type": "n8n-nodes-base.code",
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "let oferta;\ntry {\n  const texto = $json.content && $json.content[0] ? $json.content[0].text : '';\n  oferta = JSON.parse(texto);\n} catch (e) {\n  oferta = null;\n}\n\nconst linkOriginal = $('Parsear ofertas').item.json.link;\n\nif (!oferta) {\n  return { json: { error_extraccion: true, link: linkOriginal, respuesta_cruda: $json } };\n}\n\noferta.link = linkOriginal || oferta.link || null;\nreturn { json: oferta };"
      }
    },
    {
      "typeVersion": 2,
      "position": [
        1280,
        100
      ],
      "id": "aplicar-reglas",
      "name": "Aplicar reglas de decisión",
      "type": "n8n-nodes-base.code",
      "onError": "continueRegularOutput",
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "// Mirror de C:\\\\Users\\\\marco\\\\radar-empleos\\\\reglas.js — si cambiás una\n// regla, cambiala ahí primero, corré `node --test tests/`, y después pegala\n// acá. Los tests de reglas.js son la referencia de qué tiene que pasar.\nconst MAX_ANIOS_TOLERADO = 3;\nconst MAX_POSTULANTES_BAJA_COMPETENCIA = 25;\nconst MAX_ANTIGUEDAD_HORAS_BAJA_COMPETENCIA = 6;\nconst MIN_COINCIDENCIAS_STACK = 2;\n// Stack real de Marco (perfil Full Stack / Automation Engineer). Editar\n// acá a mano cuando cambie el CV.\nconst CANDIDATO_STACK = ['react', 'node', 'express', 'javascript', 'php', 'postgres', 'supabase', 'html', 'css', 'n8n'];\nconst TERMINOS_EXCLUYEN_PERFIL = ['junior', 'entry level', 'entry-level', 'recent grad', 'recent graduate', 'bootcamp', 'no bootcamp'];\n\nfunction normalizarEmpresaPuesto(empresa, puesto) {\n  const normalizar = (texto) => (texto || '')\n    .toLowerCase()\n    .normalize('NFD').replace(new RegExp('[̀-ͯ]', 'g'), '')\n    .replace(/\\b(s\\.?a\\.?|s\\.?r\\.?l\\.?|inc\\.?|ltd\\.?|llc\\.?)\\b/g, '')\n    .replace(/[^a-z0-9]+/g, ' ')\n    .trim();\n  return `${normalizar(empresa)}::${normalizar(puesto)}`;\n}\n\nfunction contarCoincidenciasStack(ofertaStack) {\n  const stackOferta = (ofertaStack || []).map((t) => t.toLowerCase());\n  return CANDIDATO_STACK.filter((tech) => stackOferta.some((t) => t.includes(tech) || tech.includes(t))).length;\n}\n\nfunction fraseExcluyePerfil(frasesExclusion) {\n  const texto = (frasesExclusion || []).join(' ').toLowerCase();\n  return TERMINOS_EXCLUYEN_PERFIL.find((t) => texto.includes(t)) || null;\n}\n\n// Dedup key única por ítem (nunca null) para que un error nunca pise ni sea\n// pisado por otra fila en el appendOrUpdate por dedup_key de \"Guardar en\n// Sheet\". sufijoUnico es param para poder testear determinísticamente en\n// reglas.js — acá siempre Date.now().\nfunction generarDedupKeyError(link, sufijoUnico) {\n  const base = link || 'sin-link';\n  const sufijo = sufijoUnico !== undefined ? sufijoUnico : Date.now();\n  return `no_parseable::${base}::${sufijo}`;\n}\n\ntry {\n  const oferta = $json;\n\n  if (oferta.error_extraccion) {\n    return {\n      json: {\n        ...oferta,\n        estado: 'NO_PARSEABLE',\n        motivo: 'El modelo no devolvió JSON válido (o la llamada falló tras reintentos) — revisar a mano.',\n        dedup_key: generarDedupKeyError(oferta.link),\n      },\n    };\n  }\n\n  const dedupKey = normalizarEmpresaPuesto(oferta.empresa, oferta.puesto);\n  const vistas = $('Leer ofertas ya vistas').all().map((i) => i.json);\n  const yaPostulado = vistas.some((v) => v.dedup_key === dedupKey && String(v.postulado).toLowerCase() === 'true');\n\n  let estado, motivo;\n\n  if (yaPostulado) {\n    estado = 'DESCARTAR';\n    motivo = 'Ya te postulaste a este puesto en esta empresa.';\n  } else if (oferta.anios_experiencia_min > MAX_ANIOS_TOLERADO && oferta.anios_excluyente === true) {\n    estado = 'DESCARTAR';\n    motivo = `Pide ${oferta.anios_experiencia_min}+ años de experiencia de forma excluyente.`;\n  } else if (oferta.titulo_excluyente === true) {\n    estado = 'DESCARTAR';\n    motivo = `Pide título (${oferta.titulo_requerido || 'sin especificar'}) de forma excluyente.`;\n  } else {\n    const fraseExcluyente = fraseExcluyePerfil(oferta.frases_exclusion);\n    if (fraseExcluyente) {\n      const cita = (oferta.frases_exclusion || []).find((f) => f.toLowerCase().includes(fraseExcluyente));\n      estado = 'DESCARTAR';\n      motivo = `El aviso excluye tu perfil: \"${cita}\"`;\n    } else {\n      const coincidencias = contarCoincidenciasStack(oferta.stack);\n      const coincidenciaReal = coincidencias >= MIN_COINCIDENCIAS_STACK;\n      const seniales = [];\n      if (oferta.postulantes !== null && oferta.postulantes !== undefined && oferta.postulantes < MAX_POSTULANTES_BAJA_COMPETENCIA) seniales.push(`${oferta.postulantes} postulantes`);\n      if (oferta.antiguedad_horas !== null && oferta.antiguedad_horas !== undefined && oferta.antiguedad_horas < MAX_ANTIGUEDAD_HORAS_BAJA_COMPETENCIA) seniales.push(`publicada hace ${oferta.antiguedad_horas}hs`);\n      if (oferta.postulacion_rapida === true) seniales.push('postulación rápida disponible');\n      const competenciaBaja = seniales.length > 0;\n\n      if (coincidenciaReal && competenciaBaja) {\n        estado = 'POSTULAR YA';\n        motivo = `Stack coincide (${coincidencias} tecnologías) y competencia baja: ${seniales.join(', ')}.`;\n      } else if (!coincidenciaReal) {\n        estado = 'MIRAR';\n        motivo = `Stack no coincide lo suficiente (${coincidencias}/${MIN_COINCIDENCIAS_STACK} tecnologías).`;\n      } else {\n        estado = 'MIRAR';\n        motivo = 'Sin datos de competencia — incierto, no se asume baja competencia.';\n      }\n    }\n  }\n\n  return { json: { ...oferta, dedup_key: dedupKey, estado, motivo, stack_join: (oferta.stack || []).join(', ') } };\n} catch (e) {\n  // Cualquier forma inesperada de la oferta (campo con tipo raro, etc.) no\n  // tira el resto del batch — se marca para revisión manual, igual que un\n  // JSON inválido del modelo.\n  return {\n    json: {\n      error_extraccion: true,\n      estado: 'NO_PARSEABLE',\n      motivo: `Error inesperado aplicando reglas: ${String((e && e.message) || e)} — revisar a mano.`,\n      dedup_key: generarDedupKeyError($json ? $json.link : null),\n      link: $json ? $json.link : null,\n    },\n  };\n}"
      }
    },
    {
      "typeVersion": 4.5,
      "position": [
        1520,
        100
      ],
      "id": "guardar-en-sheet",
      "name": "Guardar en Sheet",
      "type": "n8n-nodes-base.googleSheets",
      "parameters": {
        "operation": "appendOrUpdate",
        "documentId": {
          "mode": "id",
          "value": "TU_SHEET_ID_AQUI"
        },
        "sheetName": {
          "mode": "name",
          "value": "Ofertas"
        },
        "columns": {
          "mappingMode": "defineBelow",
          "matchingColumns": [
            "dedup_key"
          ],
          "value": {
            "fecha_detectada": "={{ $now.toISODate() }}",
            "dedup_key": "={{ $json.dedup_key }}",
            "empresa": "={{ $json.empresa }}",
            "puesto": "={{ $json.puesto }}",
            "modalidad": "={{ $json.modalidad }}",
            "tipo_contrato": "={{ $json.tipo_contrato }}",
            "horas_semanales": "={{ $json.horas_semanales }}",
            "salario_min": "={{ $json.salario_min }}",
            "salario_max": "={{ $json.salario_max }}",
            "salario_moneda": "={{ $json.salario_moneda }}",
            "anios_experiencia_min": "={{ $json.anios_experiencia_min }}",
            "stack": "={{ $json.stack_join }}",
            "postulantes": "={{ $json.postulantes }}",
            "antiguedad_horas": "={{ $json.antiguedad_horas }}",
            "confianza_extraccion": "={{ $json.confianza_extraccion }}",
            "link": "={{ $json.link }}",
            "estado": "={{ $json.estado }}",
            "motivo": "={{ $json.motivo }}",
            "fuente": "={{ $json.fuente }}"
          }
        },
        "options": {}
      },
      "credentials": {
        "googleSheetsOAuth2Api": {
          "id": "REEMPLAZAR",
          "name": "Google Sheets Account"
        }
      }
    },
    {
      "typeVersion": 2,
      "position": [
        1760,
        100
      ],
      "id": "armar-mail-resumen",
      "name": "Armar mail resumen",
      "type": "n8n-nodes-base.code",
      "onError": "continueRegularOutput",
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const items = $input.all().map((i) => i.json);\n\nconst postularYa = items.filter((o) => o.estado === 'POSTULAR YA').sort((a, b) => {\n  const distA = a.horas_semanales == null ? 999 : Math.abs(a.horas_semanales - 20);\n  const distB = b.horas_semanales == null ? 999 : Math.abs(b.horas_semanales - 20);\n  return distA - distB; // más cerca de 20hs/semana primero — preferencia de Marco, no cambia el estado\n});\nconst mirar = items.filter((o) => o.estado === 'MIRAR');\nconst noParseables = items.filter((o) => o.estado === 'NO_PARSEABLE');\nconst descartadas = items.filter((o) => o.estado === 'DESCARTAR');\n\nconst topeAlcanzado = items.length > 0 && items[0]._tope_alcanzado === true;\nconst totalDetectado = items.length > 0 ? items[0]._total_detectado : 0;\nconst procesadas = items.length > 0 ? items[0]._procesadas : 0;\n\nconst avisoTope = !topeAlcanzado ? '' : `\n  <p style=\"background:#fee2e2; padding:8px; border-radius:4px;\">⚠️ Tope de\n  seguridad alcanzado: se detectaron ${totalDetectado} ofertas, se\n  procesaron solo ${procesadas}. Revisá a mano el resto en Gmail (label\n  radar-empleos).</p>`;\n\nconst filaOferta = (o) => `\n  <tr>\n    <td><a href=\"${o.link || '#'}\">${o.puesto || '(sin puesto)'} — ${o.empresa || '(sin empresa)'}</a>${o.horas_semanales ? ` (${o.horas_semanales}hs/sem)` : ''}</td>\n    <td>${o.motivo || ''}</td>\n  </tr>`;\n\nconst filaNoParseable = (o) => `\n  <tr>\n    <td><a href=\"${o.link || '#'}\">${o.link || '(sin link)'}</a></td>\n    <td>${o.motivo || ''}</td>\n  </tr>`;\n\nconst seccion = (titulo, lista, fila) => lista.length === 0 ? '' : `\n  <h2>${titulo} (${lista.length})</h2>\n  <table cellpadding=\"6\">${lista.map(fila || filaOferta).join('')}</table>`;\n\nconst html = `\n  <div style=\"font-family: sans-serif; max-width: 600px;\">\n    ${avisoTope}\n    ${seccion('🟢 Postular ya', postularYa)}\n    ${seccion('🟡 Mirar', mirar)}\n    ${seccion('⚠️ No se pudo procesar — revisar a mano', noParseables, filaNoParseable)}\n    <p>🔴 Descartadas: ${descartadas.length}</p>\n  </div>`;\n\nreturn [{\n  json: {\n    asunto: `Radar de empleos — ${postularYa.length} para postular ya`,\n    html,\n  },\n}];"
      }
    },
    {
      "typeVersion": 2.1,
      "position": [
        2000,
        100
      ],
      "id": "enviar-mail-resumen",
      "name": "Enviar mail resumen",
      "type": "n8n-nodes-base.gmail",
      "parameters": {
        "resource": "message",
        "operation": "send",
        "sendTo": "REEMPLAZAR@ejemplo.com",
        "subject": "={{ $json.asunto }}",
        "emailType": "html",
        "message": "={{ $json.html }}",
        "options": {}
      },
      "credentials": {
        "gmailOAuth2": {
          "id": "REEMPLAZAR",
          "name": "Gmail account"
        }
      }
    },
    {
      "typeVersion": 4.2,
      "position": [
        2240,
        100
      ],
      "id": "avisar-latido-ok",
      "name": "Avisar latido OK (healthchecks.io)",
      "type": "n8n-nodes-base.httpRequest",
      "onError": "continueRegularOutput",
      "parameters": {
        "method": "GET",
        "url": "=https://hc-ping.com/TU_HEALTHCHECKS_UUID_AQUI",
        "options": {
          "timeout": 10000
        }
      }
    },
    {
      "typeVersion": 1,
      "position": [
        -220,
        -180
      ],
      "id": "nota-setup",
      "name": "Sticky Note",
      "type": "n8n-nodes-base.stickyNote",
      "parameters": {
        "width": 560,
        "height": 420,
        "content": "## ⚠️ Bug urgente encontrado (pendiente)\n\n\"Leer ofertas ya vistas\" falló en la corrida real del 28/8 9am con\n\"Requested entity was not found\" de Google Sheets — la credencial\n\"Google Sheets account\" no está viendo el Sheet `TU_SHEET_ID_AQUI`.\nRevisar: ¿la cuenta de Google conectada en esa credencial es la dueña\n(o tiene acceso) al Sheet? Sin arreglar esto el workflow no procesa nada.\n\n## Antes de importar\n\n1. Credenciales: Gmail OAuth2, Google Sheets OAuth2, Header Auth para\n   Anthropic (header `x-api-key`, valor `={{ $env.ANTHROPIC_API_KEY }}`)\n2. Reemplazar `TU_SHEET_ID_AQUI` en los 2 nodos de Google Sheets — mismo\n   documento en los dos, o \"Leer ofertas ya vistas\" no encuentra nada\n3. Reemplazar el mail de destino en \"Enviar mail resumen\"\n4. \"Parsear ofertas\" tiene un dispatcher por remitente (LinkedIn, Indeed,\n   Computrabajo validados contra mails reales; Bumeran cae a revisión\n   manual a propósito — le falta el html del mail, no el texto plano;\n   ZonaJobs no está sumado, no tenía alerta configurada) — leer el\n   comentario del Code node antes de confiar en los resultados de la\n   primera corrida\n5. Si cambia el CV, editar `CANDIDATO_STACK` a mano en \"Aplicar reglas de\n   decisión\" (y en reglas.js)\n6. Tope de ofertas por corrida: `MAX_OFERTAS_POR_CORRIDA` en \"Aplicar tope\n   de seguridad\" (default 30)\n7. Crear check en healthchecks.io (período 24hs, gracia 2hs), pegar el\n   UUID en \"Avisar latido OK\" acá y en \"Avisar fallo\" del error-handler.json\n8. Importar error-handler.json aparte, poner tu mail ahí, copiar su\n   Workflow ID, pegarlo en Settings → Error Workflow de este workflow"
      }
    }
  ],
  "connections": {
    "Cron diario": {
      "main": [
        []
      ]
    },
    "Leer alertas Gmail": {
      "main": [
        [
          {
            "node": "Parsear ofertas",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Parsear ofertas": {
      "main": [
        [
          {
            "node": "Aplicar tope de seguridad",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Aplicar tope de seguridad": {
      "main": [
        [
          {
            "node": "Preparar prompt IA",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Preparar prompt IA": {
      "main": [
        [
          {
            "node": "Extraer datos con IA",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Extraer datos con IA": {
      "main": [
        [
          {
            "node": "Parsear respuesta IA",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Marcar fallo llamada IA",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Marcar fallo llamada IA": {
      "main": [
        [
          {
            "node": "Aplicar reglas de decisión",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Parsear respuesta IA": {
      "main": [
        [
          {
            "node": "Aplicar reglas de decisión",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Aplicar reglas de decisión": {
      "main": [
        [
          {
            "node": "Guardar en Sheet",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Guardar en Sheet": {
      "main": [
        [
          {
            "node": "Armar mail resumen",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Armar mail resumen": {
      "main": [
        [
          {
            "node": "Enviar mail resumen",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Enviar mail resumen": {
      "main": [
        [
          {
            "node": "Avisar latido OK (healthchecks.io)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "errorWorkflow": "REEMPLAZAR_CON_ID_ERROR_WORKFLOW"
  }
}
